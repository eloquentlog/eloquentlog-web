@require './partial/_colour'

#header
  .content
    .global-header
      background-color $global-header-color
      border-top 3px solid $global-header-border-color
      line-height 1.32rem
      margin 0
      min-height 2.42rem
      padding 0
      text-align center
